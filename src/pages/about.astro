---
const stack = [
  "Astro",
  "Nginx",
  "Ubuntu",
  "GitHub",
  "Docker",
  "Telegram Bot",
  "Gemini",
  "Cloudflare",
];
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>关于 · Project Xiomn</title>
    <meta name="description" content="关于 Project Xiomn。" />
  </head>

  <body>
    <main class="page">
      <nav>
        <a href="/">首页</a>
        <a href="/projects">项目</a>
        <a href="/docs">文档</a>
        <a href="/status">状态</a>
      </nav>

      <section class="hero">
        <p>ABOUT</p>
        <h1>一个不断生长的 AI 实验室。</h1>
        <span>
          Project Xiomn 是一个个人技术项目，用来把 AI、Telegram Bot、云服务、自动化和开源工程整合到一起。
        </span>
      </section>

      <section class="story">
        <article>
          <strong>为什么做这个？</strong>
          <h2>把想法变成可运行的产品。</h2>
          <p>
            Project Xiomn 不是一个单独的网站，而是一个长期项目。它会记录服务器搭建、网站开发、
            Telegram AI Bot、Gemini Live 接入、自动部署和后续所有 AI 工具的构建过程。
          </p>
        </article>

        <article>
          <strong>核心方向</strong>
          <h2>AI Bot、云端服务和开源沉淀。</h2>
          <p>
            当前重点是把 Telegram AI Bot 做成真正可部署、可扩展、可交互的万能 AI 助手，
            同时把官网、文档和服务状态整理成公开入口。
          </p>
        </article>
      </section>

      <section class="section">
        <p class="label">TECH STACK</p>
        <h2>正在使用的技术栈</h2>

        <div class="tags">
          {stack.map((item) => <span>{item}</span>)}
        </div>
      </section>

      <section class="section panel">
        <p class="label">NEXT</p>
        <h2>下一阶段</h2>
        <p>
          下一步会继续完善文档系统、Telegram AI Bot 展示页、自动部署流程和多语言内容结构。
        </p>
      </section>
    </main>

    <style>
      :root {
        color-scheme: dark;
        --bg: #05060a;
        --text: rgba(255, 255, 255, 0.94);
        --muted: rgba(255, 255, 255, 0.64);
        --line: rgba(255, 255, 255, 0.14);
        --card: rgba(255, 255, 255, 0.08);
        --cyan: #77ffe1;
        --blue: #62a8ff;
        --purple: #b58cff;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        background:
          radial-gradient(circle at top left, rgba(181, 140, 255, 0.2), transparent 34rem),
          radial-gradient(circle at bottom right, rgba(119, 255, 225, 0.13), transparent 32rem),
          var(--bg);
        color: var(--text);
        font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      }

      a {
        color: inherit;
        text-decoration: none;
      }

      .page {
        width: min(1100px, calc(100% - 32px));
        margin: 0 auto;
        padding: 28px 0 80px;
      }

      nav {
        display: flex;
        gap: 20px;
        margin-bottom: 80px;
        flex-wrap: wrap;
      }

      nav a {
        color: var(--muted);
      }

      nav a:hover {
        color: var(--text);
      }

      .hero {
        margin-bottom: 70px;
      }

      .hero p,
      .label {
        color: var(--cyan);
        font-size: 13px;
        font-weight: 800;
        letter-spacing: 0.18em;
      }

      h1 {
        max-width: 1000px;
        margin: 0;
        font-size: clamp(52px, 8vw, 104px);
        line-height: 0.96;
        letter-spacing: -0.075em;
      }

      .hero span {
        display: block;
        max-width: 780px;
        margin-top: 26px;
        color: var(--muted);
        font-size: 20px;
        line-height: 1.7;
      }

      .story {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 18px;
        margin-bottom: 80px;
      }

      article,
      .panel {
        padding: 28px;
        border: 1px solid var(--line);
        border-radius: 30px;
        background: var(--card);
        backdrop-filter: blur(20px);
      }

      strong {
        color: var(--cyan);
        font-size: 13px;
      }

      h2 {
        margin: 18px 0 0;
        font-size: clamp(32px, 4vw, 54px);
        line-height: 1.05;
        letter-spacing: -0.055em;
      }

      article p,
      .panel p {
        color: var(--muted);
        line-height: 1.8;
        font-size: 17px;
      }

      .section {
        margin-top: 70px;
      }

      .tags {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-top: 28px;
      }

      .tags span {
        padding: 10px 14px;
        border: 1px solid var(--line);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.06);
        color: rgba(255, 255, 255, 0.82);
      }

      @media (max-width: 860px) {
        .story {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </body>
</html>
